name: schedule-test

on: projects_v2_item

jobs:
  schedule-test:
    if: github.repository == 'grafana/github-actions-testrepo'
    runs-on: ubuntu-latest
    steps:
    - name: schedule-test      
      run: | 
          echo "running"
          echo 'GH_EVENT='${{ toJson(github.event) }} >> $GITHUB_ENV
